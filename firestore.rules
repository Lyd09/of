
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Permite que usuários autenticados leiam e escrevam na coleção 'clients'
    match /clients/{clientId} {
      allow read, write: if request.auth != null;
    }
  }
}
